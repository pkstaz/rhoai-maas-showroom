= Probar inferencia

El modelo en MaaS se llama con API key.

== Prerrequisito: Authorino confía en maas-api

Si `/v1/models` o el chat responden `AUTH_FAILURE` / `Exception thrown while generating token` con una API key válida, Authorino no valida el certificado TLS de `maas-api`. Monta el service-CA:

[source,bash]
----
bash manifests/fix-maas-authorino-ca.sh
----

== API key

Usa el **mismo** nombre de subscription del módulo 9 (`qwen-authenticated`). Si creaste otra en la UI (p. ej. `demo`), cámbiala en el JSON.

[source,bash]
----
CLUSTER_DOMAIN=$(oc get ingresses.config/cluster -o jsonpath='{.spec.domain}')
MAAS_URL="maas.${CLUSTER_DOMAIN}"
echo "https://${MAAS_URL}"

oc get maassubscription -n models-as-a-service

API_KEY=$(curl -sk -X POST "https://${MAAS_URL}/maas-api/v1/api-keys" \
  -H "Authorization: Bearer $(oc whoami -t)" \
  -H "Content-Type: application/json" \
  -d '{"name":"workshop","subscription":"qwen-authenticated","expiresIn":"8h"}' \
  | jq -r '.key')

echo "$API_KEY"
----

Si imprime `null`, la subscription no existe o el nombre no coincide. Revisa el error completo sin `jq -r '.key'`.

== Listar modelos y chatear

IMPORTANT: El Qwen CPU del lab tiene `max_model_len=2048`. Pide **`max_tokens` ≤ 1024** (64 en smoke). Con 4096 verás `invalid_prompt` / `max_tokens cannot be greater than max_model_len`.

[source,bash]
----
curl -sk "https://${MAAS_URL}/v1/models" \
  -H "Authorization: Bearer ${API_KEY}" | jq

MODEL_ID=$(curl -sk "https://${MAAS_URL}/v1/models" \
  -H "Authorization: Bearer ${API_KEY}" \
  | jq -r '.data[0].id')

curl -sk "https://${MAAS_URL}/v1/chat/completions" \
  -H "Authorization: Bearer ${API_KEY}" \
  -H "Content-Type: application/json" \
  -d "{
    \"model\": \"${MODEL_ID}\",
    \"messages\": [{\"role\":\"user\",\"content\":\"Say hello in one sentence. /no_think\"}],
    \"max_tokens\": 64
  }" | jq
----

El `id` suele ser `publishers/llm/models/qwen3-06b`.

== UI — Models as a Service

Dashboard → *Models as a Service* → el modelo → *Playground*. Usa `/no_think` y `max_tokens` bajo (≤ 1024).

== UI — Gen AI Studio Playground

Gen AI Studio crea un `OGXServer` que por defecto usa `VLLM_API_TOKEN=fake` y `VLLM_MAX_TOKENS=4096`. Eso provoca *403* (token falso) y *invalid_prompt* (4096 > 2048).

Tras tener una subscription y API key que funcionen con `curl`:

[source,bash]
----
bash manifests/fix-playground-maas.sh
----

Luego: *Gen AI Studio* → *Playground* → proyecto `llm` → *New chat* → elige el modelo MaaS (`…/publishers/llm/models/qwen3-06b`) → mensaje con `/no_think`.

NOTE: Si el dashboard regenera el `OGXServer`, puede volver `fake` / `4096`. Vuelve a ejecutar el script.

== Listo

Tienes:

* OpenShift AI {rhoai-version}
* Fake GPU + GPU Booking
* {qwen-model} en catalog
* llm-d CPU detrás de MaaS
* Suscripción para usuarios autenticados
* Inferencia vía API key y Playground

== Siguiente

xref:11-00-observabilidad.adoc[Observabilidad MaaS]: gateway, serving, rate limits y FinOps básico.

Luego xref:12-00-evalhub.adoc[EvalHub]: smoke de calidad del modelo.
