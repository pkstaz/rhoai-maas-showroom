= Operator de OpenShift AI

== UI

. *Ecosystem* → *OperatorHub*
. Busca `Red Hat OpenShift AI`
. Elige el Operator *Red Hat* (no Community)
. Channel: `stable-3.5`
. Version: `{rhoai-version}`
. *Install*
. Installation mode: *All namespaces*
. Installed Namespace: `redhat-ods-operator` (recomendado)
. Update approval: *Automatic*
. Espera *Succeeded* (varios minutos)

== Verificar

[source,bash]
----
oc get csv -n redhat-ods-operator
oc get pods -n redhat-ods-operator
----

CSV `Succeeded`, pods `Running`.

.Alternativa YAML
[%collapsible]
====
[source,bash]
----
oc apply -f examples/rhoai-operator.yaml
oc get csv -n redhat-ods-operator -w
----
====
