= EvalHub (evaluación del modelo)

CAUTION: *WIP* (Work in Progress). Este módulo aún se valida en lab; los pasos pueden cambiar.

Prueba de calidad tras xref:11-00-observabilidad.adoc[observabilidad MaaS]. Preferido: modelo TMM del xref:08-03-deploy-external.adoc[módulo 8.3]. Fallback: Qwen CPU (módulo 8.1) + subscription del módulo 9.

Este módulo deja *Develop & train → Evaluations* usable desde la web (TrustyAI + MLflow + EvalHub).

== Qué instala

[cols="2,3"]
|===
| Pieza | Para qué

| `trustyai` Managed
| Operator TrustyAI (CRD `EvalHub`)

| `mlflowoperator` Managed + CR `mlflow`
| Tracking de experimentos (requerido por la UI Evaluations)

| Postgres efímero en `evalhub`
| Backend del EvalHub Server (lab)

| `EvalHub` CR + `MLFLOW_TRACKING_URI` (`…/mlflow`) + `MLFLOW_WORKSPACE`
| Jobs de eval + log a MLflow (el path `/mlflow` es obligatorio)

| RoleBinding EvalHub SA → proyecto `llm`
| Acceso al workspace MLflow del DSP (el experimento vive en `llm`, no en `evalhub`)

| Label `evalhub.trustyai.opendatahub.io/tenant` en `llm`
| El dashboard descubre EvalHub vía ConfigMap `evalhub-discovery` en el proyecto

| `disableLMEval: false`
| Menú *Develop & train → Evaluations*
|===

== Script (recomendado)

[source,bash]
----
bash manifests/apply-evalhub.sh
----

Aplica operators → *MLflow Ready* → EvalHub → RBAC al workspace `llm`. Hard refresh del dashboard (`Cmd+Shift+R`).

IMPORTANT: Crea el CR `mlflow` *antes* que `EvalHub`. Si EvalHub arrancó primero, borra y vuelve a aplicar el CR (`oc delete evalhub evalhub -n evalhub` y re-ejecuta el script).

== Paso a paso

=== 1. Operators en el DSC

[source,bash]
----
oc patch dsc default-dsc --type=merge -p '{
  "spec":{
    "components":{
      "trustyai":{"managementState":"Managed"},
      "mlflowoperator":{"managementState":"Managed"}
    }
  }
}'
----

Espera `TrustyAIReady=True` y `MLflowOperatorReady=True`.

=== 2. Flags del dashboard

[source,bash]
----
oc patch odhdashboardconfig odh-dashboard-config -n redhat-ods-applications --type merge -p '{
  "spec":{"dashboardConfig":{"disableLMEval":false,"genAiStudio":true}}
}'
----

=== 3. MLflow (lab: SQLite + PVC)

[source,bash]
----
oc apply -f manifests/evalhub/mlflow.yaml
oc get pods -n redhat-ods-applications | grep -i mlflow
----

Espera el deploy `mlflow` con `readyReplicas=1` *antes* del paso 4.

=== 4. EvalHub

[source,bash]
----
oc apply -k manifests/evalhub
oc label ns llm evalhub.trustyai.opendatahub.io/tenant=true --overwrite
oc apply -f manifests/evalhub/mlflow-workspace-rbac.yaml
oc get evalhub,pods,route -n evalhub
oc get cm evalhub-discovery -n llm
----

El YAML fija:

* Label tenant en `llm` + ConfigMap `evalhub-discovery` (sin eso: *Evaluations unavailable* / TrustyAI Operator)
* `MLFLOW_TRACKING_URI=https://mlflow.redhat-ods-applications.svc.cluster.local:8443/mlflow`
* `MLFLOW_WORKSPACE=llm`

Sin el sufijo `/mlflow`, CreateEvaluationJob falla con `Workspace context is required`.

== Valores para el formulario (TMM primero)

Si corriste xref:08-03-deploy-external.adoc[modelo externo], EvalHub llama al TMM (mejor modelo, sin GPU en *este* cluster). Reusa el Secret `tmm-api-key`.

[source,bash]
----
# Tras apply-external-model.sh (o re-exporta lo que imprimió):
export MODEL_ID="${MODEL_ID:-${TMM_MODEL_NAME}}"
export ENDPOINT="${ENDPOINT:-${TMM_ENDPOINT}}"
export API_KEY_SECRET="${API_KEY_SECRET:-tmm-api-key}"
export TOKENIZER="${TOKENIZER:-${TMM_TOKENIZER:-}}"

printf '\n'
printf '┌──────────────────────────────────────────────────────────────┐\n'
printf '│  EvalHub — External (TMM / instructor)                       │\n'
printf '├──────────────────────────────────────────────────────────────┤\n'
printf '│ MODEL_ID       = %-44s │\n' "${MODEL_ID}"
printf '│ ENDPOINT       = %-44s │\n' "${ENDPOINT}"
printf '│ API_KEY_SECRET = %-44s │\n' "${API_KEY_SECRET}"
printf '│ TOKENIZER      = %-44s │\n' "${TOKENIZER:-<HF repo, no el id MaaS>}"
printf '└──────────────────────────────────────────────────────────────┘\n'
----

Rotar la key de esta sala: `MODE=key API_KEY='…' bash manifests/apply-external-model.sh`.

=== Fallback — Qwen MaaS local

Solo si *no* hay TMM. Secret con la API key MaaS (clave `OPENAI_API_KEY`):

[source,bash]
----
CLUSTER_DOMAIN=$(oc get ingresses.config.openshift.io cluster -o jsonpath='{.spec.domain}')
MAAS_URL="maas.${CLUSTER_DOMAIN}"

API_KEY=$(curl -sk -X POST "https://${MAAS_URL}/maas-api/v1/api-keys" \
  -H "Authorization: Bearer $(oc whoami -t)" \
  -H "Content-Type: application/json" \
  -d '{"name":"evalhub","subscription":"qwen-authenticated","expiresIn":"72h"}' \
  | jq -r '.key')

oc create secret generic maas-eval-api-key -n llm \
  --from-literal=OPENAI_API_KEY="${API_KEY}" \
  --from-literal=api-key="${API_KEY}" \
  --dry-run=client -o yaml | oc apply -f -
# El job corre en namespace evalhub y monta el mismo Secret
oc get secret maas-eval-api-key -n llm -o yaml \
  | sed 's/namespace: llm/namespace: evalhub/' | oc apply -f -

export ENDPOINT="https://${MAAS_URL}/v1"
export MODEL_ID=$(curl -sk "https://${MAAS_URL}/v1/models" \
  -H "Authorization: Bearer ${API_KEY}" | jq -r '.data[0].id')
export API_KEY_SECRET="maas-eval-api-key"

printf '\n'
printf '┌──────────────────────────────────────────────────────────────┐\n'
printf '│  EvalHub — External model (pegar en el formulario)           │\n'
printf '├──────────────────────────────────────────────────────────────┤\n'
printf '│ MODEL_ID       = %-44s │\n' "${MODEL_ID}"
printf '│ ENDPOINT       = %-44s │\n' "${ENDPOINT}"
printf '│ API_KEY_SECRET = %-44s │\n' "${API_KEY_SECRET}"
printf '└──────────────────────────────────────────────────────────────┘\n'
----

En el formulario: *Model name* = `MODEL_ID`, *Endpoint URL* = `ENDPOINT`, *API key secret name* = `API_KEY_SECRET` (nombre del Secret, no el valor de la key).

== UI — lanzar la evaluación

IMPORTANT: El experimento MLflow se crea en el proyecto Data Science *`llm`* (donde está el modelo).
Eso debe coincidir con `MLFLOW_WORKSPACE=llm`. El namespace *`evalhub`* solo aloja el CR/servidor EvalHub y los pods del job — *no* es el workspace de Experiments.

Si *Evaluations* muestra *Evaluations unavailable* / *TrustyAI Operator*, falta el label tenant (o el ConfigMap de discovery):

[source,bash]
----
oc label ns llm evalhub.trustyai.opendatahub.io/tenant=true --overwrite
oc get cm evalhub-discovery -n llm
----

Hard refresh del dashboard tras ver el ConfigMap.

. Dashboard → *Develop & train* → *Experiments (MLflow)* → proyecto **`llm`** (no `evalhub`)
. Crea un experimento, p. ej. `qwen-smoke`
. *Develop & train* → *Evaluations* → proyecto **`llm`** → *Start evaluation run*
. Benchmark: *Helpfulness, honesty & harmlessness (HHH)*. En *Benchmark parameters* incluye el tokenizer HF (el id MaaS *no* sirve como tokenizer):

[source,json]
----
{"limit": 5, "num_fewshot": 0, "tokenizer": "<TOKENIZER o Qwen/Qwen3-0.6B si evalúas Qwen local>"}
----

. *MLflow experiment*: el que creaste
. *Source*: external — usa `MODEL_ID`, `ENDPOINT` y `API_KEY_SECRET` (TMM del 8.3, o Qwen local)
. *Evaluate*

La UI puede quedarse en *Pending* unos minutos la *primera* vez: el pod descarga `odh-ta-lmes-job` (~7 GB). Luego pasa a *Running* / *Completed* (smoke ~5–15 min en OpenTLC). *Experiments (MLflow)* para ver métricas.

Sin `tokenizer` en los parameters, el job falla al tratar el id MaaS como repo de Hugging Face (`Repo id must be in the form 'namespace/repo_name'`).

Tras el job, *Observe & monitor → Usage* mostrará el spike de tokens.

== Siguiente

xref:12-01-garak.adoc[Garak / red team]: provider `garak` + ART Chatterbox (`garak-kfp`). Luego xref:13-00-finops.adoc[FinOps].


== API (backup)

Si la UI no lista el modelo, usa API/CLI con `benchmark_id` del provider (p. ej. el id interno de HHH / `bigbench_hhh_alignment_multiple_choice`) y `limit: 5`.

== GAPs

[cols="2,2"]
|===
| Lab hoy | Prd

| MLflow SQLite + PVC 5 Gi
| Postgres + S3 / ObjectBucketClaim

| Postgres EvalHub efímero
| DB gestionada

| Smoke `limit: 5`
| Suites + umbrales + gates en pipeline

| Eval + serving en el mismo nodo
| Kueue / nodos de batch
|===

Tiempo orientativo: *35–50 min* (+ waits de operators).
