= Observabilidad MaaS

Dashboards de uso y métricas del gateway según la https://rh-aiservices-bu.github.io/rhoai-maas-guide/modules/main/07-observability.html[guía MaaS Phase 7].

En RHOAI 3.5 el stack de dashboards usa *Cluster Observability Operator* (Perses) + Tempo + OpenTelemetry. Para usage por request: Loki + MinIO (lab) + `usageLogging`.

Hazlo *después* de xref:10-00-verify.adoc[probar inferencia] (o en paralelo si el modelo ya está Ready).

== Qué instala

[cols="2,3"]
|===
| Pieza | Para qué

| Tempo Operator
| Trazas; el Monitoring de RHOAI lo requiere

| OpenTelemetry Operator
| Collector CRD; sin él falla el cascade de observabilidad

| Cluster Observability Operator (COO)
| Perses + pestaña *Observe & monitor* en el dashboard RHOAI

| DSCI `monitoring`
| MonitoringStack, ThanosQuerier, datasources, dashboards

| `MaasTenantConfig.telemetry`
| Labels por modelo / subscription en métricas del gateway

| Loki + MinIO + `usageLogging`
| Dashboards de consumo (tokens) admin/user
|===

== Script (recomendado)

Desde la raíz del repo:

[source,bash]
----
bash manifests/apply-maas-observability.sh
----

Espera CSVs `Succeeded`, LokiStack Ready y el patch de `usageLogging`. En un OpenTLC de 1 nodo puede tardar *10–20 min*.

Si el script se corta en un `oc wait` (`no matching resources found`), vuelve a lanzarlo: es idempotente y el wait ahora espera a que aparezca el CSV.

== Paso a paso (UI + CLI)

=== 1. Tempo Operator

[source,bash]
----
oc apply -k manifests/observability/tempo
oc wait csv -n openshift-tempo-operator \
  -l operators.coreos.com/tempo-product.openshift-tempo-operator="" \
  --for=jsonpath='{.status.phase}'=Succeeded --timeout=300s
----

=== 2. OpenTelemetry Operator

[source,bash]
----
oc apply -k manifests/observability/opentelemetry
oc wait csv -n openshift-opentelemetry-operator \
  -l operators.coreos.com/opentelemetry-product.openshift-opentelemetry-operator="" \
  --for=jsonpath='{.status.phase}'=Succeeded --timeout=300s
----

=== 3. Cluster Observability Operator

[source,bash]
----
oc apply -k manifests/observability/coo
oc wait csv -n openshift-cluster-observability-operator \
  -l operators.coreos.com/cluster-observability-operator.openshift-cluster-observability="" \
  --for=jsonpath='{.status.phase}'=Succeeded --timeout=300s
----

=== 4. DSCI monitoring

Sin esto, la pestaña de dashboards no tiene datasource:

[source,bash]
----
oc patch dsci default-dsci --type=merge -p '{
  "spec": {
    "monitoring": {
      "namespace": "redhat-ods-monitoring",
      "metrics": {
        "replicas": 1,
        "storage": { "size": "5Gi", "retention": "90d" }
      },
      "traces": {
        "sampleRatio": "0.1",
        "storage": { "backend": "pv", "retention": "2160h" }
      }
    }
  }
}'
oc wait --for=jsonpath='{.status.phase}'=Ready dsci/default-dsci --timeout=300s
----

=== 5. Telemetría del gateway

[source,bash]
----
oc patch maastenantconfig default-tenant -n models-as-a-service \
  --type=merge -p '{"spec":{"telemetry":{"enabled":true}}}'

oc get telemetrypolicies.extensions.kuadrant.io -n openshift-ingress
oc get telemetry.telemetry.istio.io -n openshift-ingress
----

El operator crea TelemetryPolicy + Istio Telemetry (labels `model`, `subscription`, …).

=== 6. Loki Operator

[source,bash]
----
oc apply -k manifests/observability/loki
oc wait csv -n openshift-operators-redhat \
  -l operators.coreos.com/loki-operator.openshift-operators-redhat="" \
  --for=jsonpath='{.status.phase}'=Succeeded --timeout=300s
----

=== 7. MinIO + LokiStack (lab)

StorageClass por defecto del OpenTLC: `gp3-csi` (ya en el manifiesto).

[source,bash]
----
oc apply -k manifests/observability/usage-logging
oc wait --for=condition=available deployment/minio -n redhat-ods-monitoring --timeout=300s
oc wait lokistack/usage -n redhat-ods-monitoring \
  --for=jsonpath='{.status.conditions[?(@.type=="Ready")].status}'=True --timeout=600s
----

=== 8. Usage logging

[source,bash]
----
oc patch configs.maas.opendatahub.io default --type=merge \
  -p '{"spec":{"usageLogging":true}}'

oc get envoyfilter maas-model-access-logs -n openshift-ingress
oc get opentelemetrycollector usage-logs -n redhat-ods-monitoring
oc get persesdashboard -n redhat-ods-monitoring | grep usage-logs
----

== UI — dashboards

. Dashboard OpenShift AI → *Observe & monitor* → *Dashboard* (o *Observability*)
. Abre los dashboards MaaS / usage (admin y user)
. Genera tráfico con el Playground o el `curl` del módulo 10; espera unos minutos a que Loki muestre requests

IMPORTANT: *GPU utilization* = *No data* es normal en este lab. El `{qwen-model}` corre con imagen **vLLM CPU** (`rhaii/vllm-cpu-rhel9`) y **sin** `nvidia.com/gpu` en el `LLMInferenceService`. Fake GPU (módulo 5) y DCGM están para perfiles/hardware; no alimentan el panel de GPU de este serving. Mira tokens, latencia y logs de usage en su lugar.

Si desplegaras el mismo modelo con imagen CUDA + request de GPU (real o fake), ahí sí aparecerían series DCGM.

=== Token consumption = 0 (Usage)

El panel *Usage → Token consumption* consulta Prometheus con filtro `user!=""`. Sin la label `user` en `TelemetryPolicy`, la UI muestra 0 aunque Limitador cuente tokens.

Fuerza la label (CPU o GPU; es del gateway, no del runtime):

[source,bash]
----
bash manifests/fix-maas-usage-user-label.sh
----

Luego genera tráfico (Playground o `curl` del módulo 10), espera ~1 min y refresca *Usage*. Deberías ver tokens con `user=admin` (o el username de la API key).

Verificación directa:

[source,bash]
----
oc port-forward -n redhat-ods-monitoring \
  svc/thanos-querier-data-science-thanos-querier 10902:10902 &
sleep 2
curl -sg --data-urlencode \
  'query=sum by (user,subscription,model) (increase(authorized_hits_total{user!=""}[30m]))' \
  'http://127.0.0.1:10902/api/v1/query' | jq
----

NOTE: Si `maas-controller` reconciliara el `TelemetryPolicy`, vuelve a ejecutar el script.

== Verificar

[source,bash]
----
oc get csv -n openshift-tempo-operator | grep tempo
oc get csv -n openshift-opentelemetry-operator | grep opentelemetry
oc get csv -n openshift-cluster-observability-operator | grep cluster-observability
oc get csv -n openshift-operators-redhat | grep loki
oc get crd tempostacks.tempo.grafana.com opentelemetrycollectors.opentelemetry.io \
  perses.perses.dev lokistacks.loki.grafana.com
oc get dsci default-dsci -o jsonpath='{.spec.monitoring.metrics}{"\n"}'
oc get maastenantconfig default-tenant -n models-as-a-service \
  -o jsonpath='{.spec.telemetry}{"\n"}'
----

CSVs `Succeeded`. `metrics` con `replicas`/`storage`. Telemetry `enabled: true`.

== FinOps rápido (rate limit)

Tras varias inferencias, HTTP `429` confirma Limitador. Los dashboards de usage muestran tokens por modelo/subscription cuando Loki está Ready.

[source,bash]
----
CLUSTER_DOMAIN=$(oc get ingresses.config/cluster -o jsonpath='{.spec.domain}')
MAAS_URL="maas.${CLUSTER_DOMAIN}"
# Reutiliza API_KEY y MODEL_ID del módulo 10
for i in $(seq 1 20); do
  CODE=$(curl -sk -o /dev/null -w '%{http_code}' \
    "https://${MAAS_URL}/v1/chat/completions" \
    -H "Authorization: Bearer ${API_KEY}" \
    -H "Content-Type: application/json" \
    -d '{"model":"'"${MODEL_ID}"'","messages":[{"role":"user","content":"/no_think ping"}],"max_tokens":200}')
  echo "request $i -> HTTP $CODE"
  [ "$CODE" = "429" ] && break
done
----

== GAPs

[cols="2,2"]
|===
| Lab hoy | Prd

| MinIO + Loki `1x.demo`
| ObjectBucketClaim / S3 gestionado

| Telemetry sin `captureUser` (GDPR)
| Activar solo si política lo permite

| Limitador en memoria
| Redis persistente (opcional en la guía MaaS)
|===

Redis para Limitador es opcional y no va en este lab.

Tiempo orientativo: *30–45 min* (+ waits de operators).

== Siguiente

xref:12-00-evalhub.adoc[EvalHub]: TrustyAI + smoke HHH.
