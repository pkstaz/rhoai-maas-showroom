= Desplegar Qwen3-0.6B (llm-d, CPU, MaaS)

Despliega *desde el catálogo* con llm-d, perfil CPU, y publícalo en MaaS.

Usa el hardware profile `cpu-workshop`, no `fake-h200`.

== Proyecto

. Dashboard → *Data Science Projects* → *Create*
. Name: `llm`

[source,bash]
----
oc label namespace llm \
  opendatahub.io/dashboard=true \
  opendatahub.io/generated-namespace=true \
  maas.opendatahub.io/gateway-access=true \
  --overwrite
----

== UI

. Catalog → abre *Qwen3-0.6B*
. *Deploy*
. Project: `llm`
. Serving: *Distributed Inference* / *llm-d*
. Hardware profile: `cpu-workshop`
. Replicas: `1`
. Publicar en *Models as a Service*
. *Deploy*

URI del modelo: `hf://Qwen/Qwen3-0.6B`

== Parche CPU

El template llm-d trae imagen CUDA. Este manifiesto usa vLLM CPU y quita `nvidia.com/gpu`.

[source,bash]
----
oc apply -f examples/llminferenceservice-qwen3-06b.yaml
----

Imagen `registry.redhat.io/rhaiis/vllm-cpu-rhel9:3.5.0`, 1–2 CPU, 8 Gi, `--max-model-len=2048`, gateway `maas-default-gateway`.

== Verificar

[source,bash]
----
oc get llminferenceservice -n llm
oc get pods -n llm
oc get maasmodelref -n llm -o wide
----

`LLMInferenceService` `Ready`. Pod `Running` (pull + load: varios minutos).
