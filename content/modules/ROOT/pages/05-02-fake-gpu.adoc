= Fake GPU Operator

Simula `nvidia.com/gpu` (y MIG) en un *worker solo CPU*.
Úsalo en single-node CPU, o en workers CPU de un cluster híbrido.
*No* lo uses en un single-node que ya tenga GPU real (ahí solo xref:05-01-nvidia-gpu.adoc[NVIDIA GPU Operator]).

Emula **una NVIDIA H200 141GB HBM3e** recortada con MIG:

[cols="1,1,1"]
|===
|Slice |Recurso |Cantidad

|1g.18gb
|`nvidia.com/mig-1g.18gb`
|2

|2g.35gb
|`nvidia.com/mig-2g.35gb`
|1

|3g.71gb
|`nvidia.com/mig-3g.71gb`
|1
|===

El worker también anuncia la GPU completa (`nvidia.com/gpu=1`) para GPU Booking y el profile `fake-h200`.

== Elegir un nodo CPU

[source,bash]
----
export CPU_WORKER_NODES=$(oc get nodes -l node-role.kubernetes.io/worker -o json | jq -r '
  .items[] |
  select((.status.allocatable["nvidia.com/gpu"] // "0") == "0"
    or .status.allocatable["nvidia.com/gpu"] == null) |
  .metadata.name
' | tr '\n' ' ')

export FAKE_GPU_NODE=$(echo "$CPU_WORKER_NODES" | awk '{print $1}')
echo "FAKE_GPU_NODE=${FAKE_GPU_NODE}"
test -n "$FAKE_GPU_NODE" && echo "OK: nodo = $FAKE_GPU_NODE" || echo "SKIP: no hay worker CPU libre"
----

Si `FAKE_GPU_NODE` está vacío, omite el resto de este menú.

Comprueba que no tenga GPU real anunciada:

[source,bash]
----
oc get node "$FAKE_GPU_NODE" -o jsonpath='{.status.allocatable.nvidia\.com/gpu}{"\n"}'
----

Debe estar vacío o `0`.

== Etiquetar nodo y namespace

[source,bash]
----
oc label node "$FAKE_GPU_NODE" run.ai/simulated-gpu-node-pool=default --overwrite

oc create namespace gpu-operator
oc label ns gpu-operator \
  pod-security.kubernetes.io/enforce=privileged \
  pod-security.kubernetes.io/audit=privileged \
  pod-security.kubernetes.io/warn=privileged \
  --overwrite
----

== Instalar Fake GPU (Helm)

Namespace `gpu-operator` = Fake (distinto de `nvidia-gpu-operator`). Desde la raíz del repo. *Siempre* pasa `-n gpu-operator` (el proyecto actual de `oc` puede ser otro).

Single-node CPU (sin NVIDIA GPU Operator):

[source,bash]
----
helm upgrade -i gpu-operator \
  oci://ghcr.io/run-ai/fake-gpu-operator/fake-gpu-operator \
  --namespace gpu-operator \
  --version 0.2.0 \
  -f manifests/fake-gpu-values.yaml
----

=== Si NVIDIA GPU Operator ya está

`helm upgrade` falla: el ClusterRole `nvidia-device-plugin` ya lo posee el GPU Operator real. No lo pises. Renderiza el chart y aplica *sin* ese ClusterRole/Binding:

[source,bash]
----
helm pull oci://ghcr.io/run-ai/fake-gpu-operator/fake-gpu-operator \
  --version 0.2.0 --untar --destination /tmp/fake-gpu-chart

helm template gpu-operator /tmp/fake-gpu-chart/fake-gpu-operator \
  -n gpu-operator -f manifests/fake-gpu-values.yaml \
  | awk 'BEGIN{skip=0} /^kind: ClusterRole(Binding)?$/{k=$0} /^  name: nvidia-device-plugin$/{skip=1} /^---$/{skip=0} !skip' \
  | oc apply -n gpu-operator -f -
----

El device-plugin Fake puede programarse en el nodo con GPU *real*. Fíjalo al pool simulado:

[source,bash]
----
oc patch ds device-plugin -n gpu-operator --type merge -p '{
  "spec":{"template":{"spec":{"nodeSelector":{"run.ai/simulated-gpu-node-pool":"default"}}}}
}'
oc patch ds nvidia-dcgm-exporter -n gpu-operator --type merge -p '{
  "spec":{"template":{"spec":{"nodeSelector":{"run.ai/simulated-gpu-node-pool":"default"}}}}
}'
oc delete pod -n gpu-operator -l app=device-plugin --ignore-not-found
----

Si cambias la topología *después* del install:

[source,bash]
----
oc delete cm "topology-${FAKE_GPU_NODE}" -n gpu-operator --ignore-not-found
oc delete pod -n gpu-operator -l app=device-plugin
oc delete pod -n gpu-operator -l app=status-updater
----

== Verificar

`$FAKE_GPU_NODE` debe seguir definido en esta sesión (si abriste otra terminal, vuelve a exportarlo).

[source,bash]
----
: "${FAKE_GPU_NODE:?FAKE_GPU_NODE vacío — vuelve a la sección Elegir un nodo CPU}"

oc get pods -n gpu-operator

oc get node "$FAKE_GPU_NODE" -o json | jq -r '
  [
    .metadata.name,
    (.metadata.labels["nvidia.com/gpu.product"] // "-"),
    (.status.allocatable["nvidia.com/gpu"] // "-"),
    (.status.allocatable["nvidia.com/mig-1g.18gb"] // "-"),
    (.status.allocatable["nvidia.com/mig-2g.35gb"] // "-"),
    (.status.allocatable["nvidia.com/mig-3g.71gb"] // "-")
  ] | @tsv
' | awk -F'\t' 'BEGIN{
  printf "%-42s %-28s %-8s %-10s %-10s %-10s\n","NAME","PRODUCT","GPU","MIG1","MIG2","MIG3"
}
{ printf "%-42s %-28s %-8s %-10s %-10s %-10s\n",$1,$2,$3,$4,$5,$6 }'
----

Esperado: `NVIDIA-H200-141GB-HBM3e`, `nvidia.com/gpu=1`, y los slices MIG.

== Hardware profiles

Profiles de este camino (no incluye `nvidia-gpu` ni `cpu-workshop`):

* `fake-h200`: 1× `nvidia.com/gpu`
* `fake-h200-mig`: 1× `nvidia.com/mig-1g.18gb`

[source,bash]
----
oc apply -f manifests/hardware-profiles-fake.yaml
----

Verificar:

[source,bash]
----
oc get hardwareprofile -n redhat-ods-applications fake-h200 fake-h200-mig
----

== Dashboard Observe (DCGM Grafana)

Panel *NVIDIA DCGM Exporter Dashboard* en *Observe* → *Dashboards* + `ServiceMonitor` del exporter Fake
(https://github.com/rhpds/gpu-resource-mgmt-automation/tree/main/workloads/fake-gpu-operator/dashboard[fuente rhpds]).

Eso **no** es el plugin *Compute* → *GPUs* (ese va en xref:05-01-nvidia-gpu.adoc[NVIDIA GPU Operator]).

[source,bash]
----
oc apply -k manifests/fake-gpu-dashboard
----

Verificar:

[source,bash]
----
oc get cm nvidia-dcgm-exporter-dashboard -n openshift-config-managed
oc get servicemonitor -n gpu-operator nvidia-dcgm-exporter
----

Consola → *Observe* → *Dashboards* → *NVIDIA DCGM Exporter Dashboard*.

Siguiente (opcional): xref:05-03-gpu-config.adoc[GPU Config Plugin] para cambiar topología/MIG Fake desde la consola.
