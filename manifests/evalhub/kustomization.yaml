apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - postgres.yaml
  - evalhub.yaml
# mlflow.yaml (redhat-ods-applications) and mlflow-workspace-rbac.yaml (needs DSP ns)
# are applied by manifests/apply-evalhub.sh
